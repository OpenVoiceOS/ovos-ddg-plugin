# TODO

## Open issues

- [ ] #7 Dependency Dashboard (Renovate bot tracking issue)

## Gaps

- [ ] `pyproject.toml` `Homepage` URL points at `OpenVoiceOS/ovos-ddg-solver-plugin` instead of `OpenVoiceOS/ovos-ddg-plugin`.
- [ ] No skill-check workflow — not required (this is an agent plugin, not a skill); `opm-check.yml` is present and appropriate.

(Tests present: `tests/test_ddg.py`, 63 test functions. CI present: build-tests, coverage, license_check, lint, opm-check, pip_audit, publish_stable, release-preview, release_workflow, repo-health — all referencing `OpenVoiceOS/gh-automations@dev`. README and pyproject present. No committed scratch artifacts.)

## Code TODOs

None found.
